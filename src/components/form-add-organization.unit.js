import FormAddOrganization from './form-add-organization'

describe('@components/form-add-organization', () => {
  it('exports a valid component', () => {
    expect(FormAddOrganization).toBeAComponent()
  })
})
